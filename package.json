{
  "name": "news-web",
  "version": "1.0.0",
  "description": "News website with browser push notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "seed:force": "node seed.js --force",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "firebase": "^12.11.0",
    "uuid": "^9.0.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
