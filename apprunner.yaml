version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install --production
run:
  command: node server.js
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
